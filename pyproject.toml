[project]
name = "aws-dbt-snowflake"
version = "0.1.0"
description = "dbt transformation pipeline with Snowflake"
readme = "README.md"
authors = [
    { name = "js449", email = "jsingh284@yahoo.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.8.0",
    "dbt-snowflake>=1.8.0",
    "sqlfmt>=0.0.3",
]

[tool.uv]
package = false
